{
  "name": "qrspi-agent",
  "version": "1.0.0",
  "description": "QRSPI Agent — structured programming agent workflow framework",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:cli": "npm run build --workspace=packages/qrspi",
    "build:mcp": "npm run build --workspace=@qrspi/mcp",
    "test": "npm test --workspace=packages/qrspi",
    "lint": "npm run lint --workspaces",
    "qrspi": "npm run build:cli && node packages/qrspi/dist/cli/main.js"
  },
  "keywords": [
    "qrspi",
    "crispy",
    "ai-agent",
    "workflow",
    "cli"
  ],
  "author": "Brian Xie",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nixihz/qrspi-agent.git"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0"
  }
}
